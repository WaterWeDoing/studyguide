import HeadingComponent from "./components/HeadingComponent";
import {Text} from "@chakra-ui/react";

const Strings = () => {
  return (
    <>
      <HeadingComponent />
      <Text>This is the string page</Text>
    </>
  );
};

export default Strings;
