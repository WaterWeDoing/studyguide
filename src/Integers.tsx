import HeadingComponent from "./components/HeadingComponent";
import {Text} from "@chakra-ui/react";

const Integers = () => {
  return (
    <>
      <HeadingComponent />
      <Text>This is the Integers Page</Text>
    </>
  );
};

export default Integers;
